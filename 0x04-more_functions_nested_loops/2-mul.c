#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: the first number to multiply
 * @b: the second number to multiply with the first number
 *
 * Return: results of the first num multiply by second num
 */

int mul(int a, int b)
{
	return (a * b);
}
